import Controller from '../controller';

const state = {
	covering: []
}

export default class SelectableController extends Controller {
	constructor(element, options) {
		super(element, {});

		
	}


	static createSelectableElement(element) {

	}
}

/**
 * vd-cover-start
 * vd-cover
 * vd-cover-end
 * 
 * vd-cover-enter
 * vd-cover-over
 * vd-cover-leave
 */